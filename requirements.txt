# Deep Fake Detection System — production dependencies
#
# REQUIRES Python 3.10, 3.11, or 3.12 (Django 5.1 will NOT install on 3.9 or older).
# Check: python3 --version
#
# If pip says "No matching distribution found for Django==5.1.5":
#   rm -rf venv && python3.11 -m venv venv && source venv/bin/activate
#   python -m pip install --upgrade pip
#   pip install -r requirements.txt
#
# Stuck on Python 3.9 only? Use: pip install -r requirements-py39.txt
#
# Install:
#   python3.11 -m venv venv
#   source venv/bin/activate   # Linux/macOS
#   pip install -r requirements.txt
#
# Local dev extras (live reload, tailwind CLI helpers):
#   pip install -r requirements-dev.txt
#
# System packages (Linux VPS) — not installed by pip:
#   sudo apt install python3-dev build-essential ffmpeg libsndfile1 libgl1 \
#     libglib2.0-0 libimage-exiftool-perl exiv2
# DB: uses PyMySQL (pip only). For mysqlclient instead: install mariadb-devel, then
#   pip install mysqlclient && set DB_USE_MYSQLCLIENT=1 in .env

# --- Web framework ---
Django==5.1.5
PyMySQL==1.1.1
gunicorn==23.0.0
python-dotenv==1.0.1
whitenoise==6.8.2

# --- ML / numerics (TensorFlow 2.15 keeps best compatibility with legacy .h5 models) ---
tensorflow==2.15.1
keras>=3.4.1,<4
numpy>=1.26.4,<2.0

# --- Media processing ---
opencv-python-headless==4.10.0.84
librosa==0.10.2.post1
soundfile==0.12.1
audioread==3.0.1

# --- Image / signal / reports ---
Pillow==11.0.0
matplotlib==3.9.2
scikit-image==0.24.0
ImageHash==4.3.1
reportlab==4.2.5
