# Production .env — copy to .env on the VPS (never commit .env)

DJANGO_DEBUG=False
DJANGO_SECRET_KEY=generate-a-long-random-string-here
DJANGO_ALLOWED_HOSTS=deepfake.yourdomain.com,127.0.0.1,localhost
CSRF_TRUSTED_ORIGINS=https://deepfake.yourdomain.com,http://deepfake.yourdomain.com

DB_NAME=ml_db
DB_USER=deepfake_user
DB_PASSWORD=strong-password-here
DB_HOST=127.0.0.1
DB_PORT=3306

# Linux VPS (default): system exiftool from apt install libimage-exiftool-perl
# EXIFTOOL_PATH=/usr/bin/exiftool
